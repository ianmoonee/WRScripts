- scripts like ccn-updater and same as have different versions regarding if it is BL or BSP, should be merged
- create README files for each folder explaining the scripts inside and how to use
- rename scripts

updates for ccn-updater :
- add WASSP_PATH ambient var instead of calling script inside wassp directory
- change "update-first-only" to "update-most-recent"
- also see workaround for 4000 chars limit on ccr
